'use client';

// Re-export react-hook-form hooks and utilities
export {
  useForm,
  useFormContext,
  useController,
  useWatch,
  useFieldArray,
  FormProvider,
} from 'react-hook-form';

// Re-export types
export type {
  UseFormProps,
  UseFormReturn,
  FieldValues,
  SubmitHandler,
  FieldError,
  UseControllerProps,
  UseWatchProps,
  UseFieldArrayProps,
} from 'react-hook-form';
